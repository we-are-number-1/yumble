{
  "name": "yumble",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd backend && npm install --only=dev && npm install && npm run build && cd ../frontend && npm install && npm run build && cp -R build ../backend/dist/build && cd ..",
    "start": "cd backend && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
